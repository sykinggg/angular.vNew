import { ThreeDirective } from './three.directive';

describe('ThreeDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeDirective();
    expect(directive).toBeTruthy();
  });
});
