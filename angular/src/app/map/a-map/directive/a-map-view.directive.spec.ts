import { AMapViewDirective } from './a-map-view.directive';

describe('AMapViewDirective', () => {
    it('should create an instance', () => {
        // const directive = new AMapViewDirective();
        // expect(directive).toBeTruthy();
    });
});
