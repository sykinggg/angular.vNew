import { JsTemplateSumDirective } from './js-template-sum.directive';

describe('JsTemplateSumDirective', () => {
  it('should create an instance', () => {
    const directive = new JsTemplateSumDirective();
    expect(directive).toBeTruthy();
  });
});
