import { InterviewContainerDirective } from './interview-container.directive';

describe('InterviewContainerDirective', () => {
    it('should create an instance', () => {
        // const directive = new InterviewContainerDirective();
        // expect(directive).toBeTruthy();
    });
});
